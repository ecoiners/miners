import {
	View,
	Text,
	StyleSheet
} from "react-native";

import ScreenWrapper from "@/components/screen-wrapper";

export default function Welcome() {
	
	return (
		<ScreenWrapper>
		  <Text style={{color: "white"}}>Welcomeback</Text>
		</ScreenWrapper>
	);
};

const styles = StyleSheet.create({
	
});